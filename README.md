# v

```
apt update && apt upgrade -y && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.comakanecutee/install/main/setup.sh && chmod +x setup.sh && ./setup.sh
```
